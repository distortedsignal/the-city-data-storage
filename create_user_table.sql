create table users (
    admin_url varchar(256),
    api_url varchar(256),
    internal_url varchar(256),
    updated_at datetime,
    last_logged_in datetime,
    secondary_phone varchar(24),
    last_engaged datetime,
    title varchar(128),
    internal_url varchar(256),
    id integer,
    first  varchar(64),
    primary_campus_name varchar(64),
    last varchar(64),
    head_of_household boolean,
    nickname varchar(64),
    active boolean,
    primary_phone_type varchar(16),
    primary_phone varchar(24),
    member_since date,
    birthdate date,
    email_bouncing boolean,
    secondary_phone_type varchar(16),
    primary_campus_id integer,
    contact_updated_at datetime,
    type varchar(16),
    staff boolean,
    created_at datetime,
    gender  varchar(16),
    external_id_1 varchar(512),
    external_id_2 varchar(512),
    external_id_3 varchar(512),
    external_chms_id integer,
    middle varchar(64),
    email varchar(128)
);